version: '3'
services:
  master.puppet.vm:
    build: .
    ports:
      - "8140:8140"
    cap_add:
      - SYS_ADMIN
    security_opt:
      - seccomp:unconfined
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
